version: "3"

services:
  django:
    build: .
    ports:
      - "8001:8000"
    volumes:
      - ./django_app:/app
    depends_on:
      - nginx

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
